# Core ML
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121

# Data + processing
numpy>=1.24
opencv-python-headless>=4.8
Pillow>=10.0

# Training / evaluation
matplotlib>=3.7
scikit-learn>=1.3

# App / utils
flask>=3.0
tqdm>=4.66

# Optional (only if you actually use synthetic data)
trdg>=1.8

# Fix broken dependency explicitly
arabic-reshaper>=2.1.4